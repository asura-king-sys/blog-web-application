<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Homepage</title>
</head>
<body>
    <h1>Latest Blog Posts</h1>
    
    <p><a href="/new">Create a New Post</a></p>
    
    <hr>

    <% if (posts.length === 0) { %>
        <p>No posts have been published yet. Be the first to write one!</p>
    <% } else { %>
        
        <% posts.slice().reverse().forEach(function(post) { %>
            <div class="blog-post">
                <h2><%= post.title %></h2> 
                
                <p>
                    <%= post.body %>
                </p>
                
                <p class="post-actions">
                    <a href="/edit/<%= post.id %>">✏️ Edit Post</a>
                    
                    <form action="/delete/<%= post.id %>" method="POST" style="display:inline; margin-left: 10px;">
                        <button type="submit" style="color: red; border: none; background: none; cursor: pointer;"
                                onclick="return confirm('Are you sure you want to delete this post?');">
                            🗑️ Delete Post
                        </button>
                    </form>
                </p>
                
                <hr> 
            </div>
        <% }); %>
        
    <% } %>

</body>
</html>