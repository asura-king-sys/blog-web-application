<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link type="Stylesheet" href="/styles/main.css">
    <title>Blog Homepage</title>
</head>
<body>
    <div class="container">
        <h1>Latest Blog Posts</h1>
        
        <a href="/new" class="create-btn">+ Create a New Post</a>

        <% if (posts.length === 0) { %>
            <div class="blog-post">
                <p>No posts have been published yet. Be the first to write one!</p>
            </div>
        <% } else { %>
            
            <% posts.slice().reverse().forEach(function(post) { %>
                <div class="blog-post">
                    <h2><%= post.title %></h2> 
                    <p><%= post.body %></p>
                    
                    <div class="post-actions">
                        <a href="/edit/<%= post.id %>" class="edit-link">✏️ Edit Post</a>
                        
                        <form action="/delete/<%= post.id %>" method="POST" style="display:inline;">
                            <button type="submit" class="delete-btn" style="border: none; background: none; cursor: pointer;"
                                    onclick="return confirm('Are you sure you want to delete this post?');">
                                🗑️ Delete Post
                            </button>
                        </form>
                    </div>
                </div>
            <% }); %>

            <footer>
                <p>Copyright © <%= new Date().getFullYear() %> | Developed by Hardik Bajpai</p>
            </footer>
        <% } %>
    </div>
</body>
</html>